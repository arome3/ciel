import { SettingsTabs } from "@/components/settings/SettingsTabs"

export const metadata = {
  title: "Settings — Ciel",
  description: "Configure your Ciel preferences",
}

export default function SettingsPage() {
  return <SettingsTabs />
}
