{
  "name": "@ciel/cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "ciel": "./src/index.ts"
  },
  "scripts": {
    "dev": "bun run src/index.ts",
    "build": "bun build src/index.ts --compile --outfile dist/ciel",
    "start": "bun run src/index.ts",
    "test": "bun test",
    "typecheck": "bunx tsc --noEmit"
  },
  "dependencies": {
    "commander": "^12.0.0",
    "viem": "^2.0.0"
  },
  "devDependencies": {
    "bun-types": "latest",
    "typescript": "^5.3.0"
  }
}
